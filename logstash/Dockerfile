ARG STACK_VERSION
FROM docker.elastic.co/logstash/logstash:$STACK_VERSION

WORKDIR /usr/share/logstash

ARG LOGSTASH_PASSWORD
ARG ES01_HOST
ENV LOGSTASH_PASSWORD=$LOGSTASH_PASSWORD
ENV ES01_HOST=$ES01_HOST